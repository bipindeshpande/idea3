# Files to Remove - Comprehensive Audit Report

## Summary

This report identifies files that are **NOT REQUIRED** for production and can be safely removed from the repository. Files are categorized by type and priority.

---

## 🔴 HIGH PRIORITY - Remove Immediately

### Generated/Build Artifacts (Should be in .gitignore)
These files are generated during builds/tests and should NOT be committed:

1. **`backend_v2/htmlcov/`** (entire directory)
   - ✅ Already in .gitignore
   - Generated HTML coverage reports
   - Can be deleted locally (regenerated with `pytest --cov-report=html`)

2. **`backend_v2/coverage.xml`**
   - ✅ Already in .gitignore
   - XML coverage report (284 KB)
   - Generated by pytest, regenerated on test run

3. **`backend_v2/mylog.log`**
   - ✅ Already in .gitignore
   - Application log file (987 KB)
   - Generated at runtime, should not be in repo

4. **`backend_v2/test.db`**
   - ✅ Already in .gitignore
   - SQLite test database (0 bytes, empty file)
   - Temporary test database file

5. **`frontend/dist/`** (entire directory)
   - ✅ Now in frontend/.gitignore
   - Vite build output
   - Generated during build process

6. **`backend_v2/.pytest_cache/`** (entire directory)
   - ✅ Already in .gitignore
   - Pytest cache directory
   - Generated during test runs

---

## 🟡 MEDIUM PRIORITY - Development/Debug Scripts

These are standalone development/debugging scripts not used by production or tests:

### Backend Root-Level Test Scripts
**Location:** `backend_v2/`

1. **`test_chunking.py`** (13 KB)
   - Standalone test script for debugging chunking logic
   - Not part of the test suite (not in `tests/` directory)
   - Not referenced by CI/CD or any production code
   - **Recommendation:** Move to `tests/` if useful, otherwise DELETE

2. **`test_logging.py`** (1.5 KB)
   - Standalone script to test file logging functionality
   - Manual testing/debugging script
   - Not referenced anywhere
   - **Recommendation:** DELETE (functionality already tested in test suite)

3. **`test_sse_chunks.py`** (5.4 KB)
   - Standalone script to simulate SSE chunk boundaries
   - Development/debugging tool
   - Not referenced anywhere
   - **Recommendation:** DELETE or move to docs/examples

4. **`test_streaming.py`** (7.5 KB)
   - Standalone script for debugging workflow_stream() buffering
   - Development/debugging tool
   - Not referenced anywhere
   - **Recommendation:** DELETE or move to docs/examples

5. **`verify_static_engine.py`** (6.4 KB)
   - Verification script for static_engine functionality
   - Manual verification tool
   - Not referenced by CI/CD
   - **Recommendation:** DELETE (functionality tested in test suite)

---

## 🟢 LOW PRIORITY - Documentation Files

These are documentation files that may be redundant or temporary:

### Backend Documentation (Root Level)
**Location:** `backend_v2/`

1. **`TEST_COVERAGE_SUMMARY.md`** (12.5 KB)
   - Summary of test coverage status
   - **Recommendation:** KEEP if actively maintained, DELETE if outdated
   - Check if information is duplicated in other docs

2. **`COVERAGE_GAPS_SUMMARY.md`** (6.8 KB)
   - Summary of test coverage gaps
   - **Recommendation:** KEEP if actively maintained, DELETE if outdated
   - Likely regenerated by scripts

3. **`HOW_TO_INCREASE_TEST_COVERAGE.md`** (14.7 KB)
   - Guide on increasing test coverage
   - **Recommendation:** KEEP if actively maintained
   - Useful reference document

4. **`PENDING_API_TESTS.md`** (6.3 KB)
   - List of pending API tests
   - **Recommendation:** KEEP if actively maintained, DELETE if all tests completed
   - Check if tasks are completed

5. **`README_TEST_DB.md`** (2.7 KB)
   - Documentation for test database setup
   - **Recommendation:** KEEP (useful reference)

### Frontend Documentation
**Location:** `frontend/`

1. **`TEST_COVERAGE_SUMMARY.md`** (4.3 KB)
   - Frontend test coverage summary
   - **Recommendation:** KEEP if actively maintained

2. **`TEST_FAILURE_ANALYSIS.md`** (7.4 KB)
   - Test failure analysis
   - **Recommendation:** DELETE if outdated, KEEP if recent failures documented

3. **`TEST_STATISTICS.md`** (5.0 KB)
   - Test statistics
   - **Recommendation:** KEEP if actively maintained

4. **`PAGE_STRUCTURE.md`** (25.4 KB)
   - Page structure documentation
   - **Recommendation:** KEEP (useful reference)

### Static Engine Documentation
**Location:** `backend_v2/app/static_engine/`

1. **`OLD_FILES_ANALYSIS.md`**
   - Analysis of old files (metadata about files that can be deleted)
   - **Recommendation:** DELETE if analysis is complete and actioned

---

## ✅ FILES TO KEEP

### Required Files (DO NOT DELETE)

#### Backend
- All files in `backend_v2/app/` (application code)
- All files in `backend_v2/tests/` (test suite)
- All files in `backend_v2/migrations/` (database migrations)
- All files in `backend_v2/scripts/` (utility scripts)
- `backend_v2/requirements.txt` (dependencies)
- `backend_v2/pytest.ini` (test configuration)
- `backend_v2/alembic.ini` (migration configuration)
- Docker files (`Dockerfile`, `Dockerfile.prod`, `Dockerfile.worker`)
- `docker-compose.yml`, `docker-compose.prod.yml`
- Deployment scripts (`deploy.sh`, `deploy.ps1`, `run_tests.sh`, `run_tests.ps1`)
- Configuration files (`pytest.ini`, `alembic.ini`)
- `backend_v2/README.md` (main documentation)
- Files in `backend_v2/docs/` (documentation)
- `backend_v2/production.env.example` (template)

#### Frontend
- All files in `frontend/src/` (source code)
- All files in `frontend/tests/` (test suite)
- `frontend/package.json`, `package-lock.json`
- `frontend/vite.config.js`, `vitest.config.js`, `playwright.config.js`
- `frontend/tailwind.config.js`, `postcss.config.js`
- `frontend/vercel.json` (deployment config)
- `frontend/index.html` (source template - different from dist/index.html)
- Files in `frontend/public/` (public assets)

---

## 📋 Action Plan

### Immediate Actions (High Priority)

1. **Delete generated files:**
   ```bash
   # Backend
   rm -rf backend_v2/htmlcov/
   rm backend_v2/coverage.xml
   rm backend_v2/mylog.log
   rm backend_v2/test.db
   
   # Frontend (if exists locally)
   rm -rf frontend/dist/
   ```

2. **Verify .gitignore is working:**
   - All files above should already be ignored
   - Run `git status` to confirm they're not tracked

### Medium Priority Actions

3. **Review and remove development scripts:**
   ```bash
   # Option 1: Delete (recommended if not needed)
   rm backend_v2/test_chunking.py
   rm backend_v2/test_logging.py
   rm backend_v2/test_sse_chunks.py
   rm backend_v2/test_streaming.py
   rm backend_v2/verify_static_engine.py
   
   # Option 2: Move to docs/examples (if want to keep for reference)
   mkdir -p docs/examples
   mv backend_v2/test_*.py docs/examples/
   mv backend_v2/verify_*.py docs/examples/
   ```

### Low Priority Actions

4. **Review documentation files:**
   - Check `TEST_COVERAGE_SUMMARY.md` - update or delete if outdated
   - Check `COVERAGE_GAPS_SUMMARY.md` - update or delete if outdated
   - Check `PENDING_API_TESTS.md` - delete if all tests completed
   - Check `TEST_FAILURE_ANALYSIS.md` - delete if outdated
   - Check `OLD_FILES_ANALYSIS.md` - delete if analysis complete

---

## 📊 Size Impact

If all recommended files are removed:
- **Generated files:** ~1.3 MB (htmlcov/, coverage.xml, mylog.log)
- **Development scripts:** ~34 KB (test_*.py, verify_*.py)
- **Total:** ~1.3 MB of unnecessary files removed

---

## ⚠️ Important Notes

1. **.gitignore Status:**
   - Most generated files are already in .gitignore
   - These files won't be committed even if they exist locally
   - Deleting them locally keeps the workspace clean

2. **Test Scripts:**
   - The standalone test scripts (test_*.py in root) are NOT part of the test suite
   - They're development/debugging tools
   - The actual test suite is in `backend_v2/tests/` directory

3. **Documentation Files:**
   - Review each documentation file before deleting
   - Some may contain useful information even if outdated
   - Consider archiving instead of deleting if unsure

4. **Backup:**
   - Consider creating a backup branch before mass deletion
   - Or delete files one category at a time

---

## ✅ Verification

After cleanup, verify:
1. All tests still pass: `pytest` and `npm test`
2. Application still builds: `docker-compose build`
3. Application still runs: `docker-compose up`
4. No broken imports or references
5. Git status shows clean working directory (except for ignored files)


